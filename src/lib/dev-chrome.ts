/**
 * Studio / brand-mocks chrome (BETA, E+F, Archive A&C tabs, desk footer, mock toasts).
 * Visible only during local `npm run dev`. Hidden on Vercel production + preview builds.
 */
export const showDevChrome = import.meta.env.DEV;
