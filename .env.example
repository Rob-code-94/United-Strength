# United Strength — local / staging env (copy to `.env.local`; never commit secrets)

# Production homepage is Direction V1 (Todd PDF edits). EF remains in DEV switcher only.

# Mariana Tek embeds (sandbox until Oct 1 evening cutover)
VITE_MARIANA_TENANT=unitedstrength.sandbox
VITE_MARIANA_LOCATION_ID=48730
VITE_MARIANA_REGION_ID=48547

# Oct 1 8:00 AM EST — set true, rebuild/redeploy; evening set false + production tenant IDs
VITE_MAINTENANCE_MODE=false

# Production Mariana values (replace after Nick issues them — do not commit real secrets if rotated)
# VITE_MARIANA_TENANT=unitedstrength
# VITE_MARIANA_LOCATION_ID=
# VITE_MARIANA_REGION_ID=
